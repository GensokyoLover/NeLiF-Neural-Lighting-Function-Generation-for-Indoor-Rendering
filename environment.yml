name: rendering_env
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - scipy
  - matplotlib
  - pytorch=2.3.1
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1 
  - tensorboard
  - scikit-image
  - pip:
      - lpips           
      - pyexr           
      - cmapy            
      - pyrr             